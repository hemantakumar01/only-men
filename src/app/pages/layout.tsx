import Navbar from "@/components/Navbar";
import Footer from "../admin/dashboard/components/components-page/Footer";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <div>
      <Navbar />
      {children}
      <Footer />
    </div>
  );
}
